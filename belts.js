// class belts {
//     constructor(rank, strength, hitPoints) {
//       this.rank = rank;
//       this.strength = strength;
//       this.hitPoints = hitPoints;
//     };

    // const seeRed => (this.rank = white ? skip turn : ignore)

    // const = whiteBelt (this.hitpoints && this.strength = i++ hitpoints && strength)
    // const = blueBelt (whiteBelt i++ (hitpoints && strength))
    // const = purpleBelt (blueBelt i++ (hitpoints && strength))
    // const = brownBelt (purpleBelt i++ (hitpoints && strength))
    // const = blackBelt (brownBelt i++ (hitpoints && strength))